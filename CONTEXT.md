# Snap-Solver

Snap-Solver 帮助用户截取题目并阅读 AI 流式生成的解答，也允许用户围绕同一道题继续追问。

## Language

**解答卡片**:
承载当前题目的思考过程和主解答正文，并采用 v1.5.1 的卡片式视觉和阅读体验。加载、生成、完成、停止和报错都是同一张卡片的状态；停止或报错不会清除已生成的正文。它位于现有解答流程内，不包含返回、生成状态、题目摘要、模型设置或同题追问等外围能力。
_Avoid_: 回答页面、AI 处理信息页面

**同题追问**:
用户在保留当前题目和主解答上下文的前提下继续提出的问题；更换题目或重解后清空。每轮用户问题以气泡呈现，AI 回答使用独立的紧凑解答卡片，不并入主解答卡片。
_Avoid_: 新问题、新会话

**阅读锁定**:
用户在流式生成期间主动上翻后保持其阅读位置的状态；新内容和生成完成事件均不得改变该位置，只有用户点击“回到最新”才解除。只要下方仍有未读内容，“回到最新”入口在生成结束后也继续存在；未进入阅读锁定时，解答完成后停留在内容底部。
_Avoid_: 暂停生成、滚动卡住

**解答流**:
在同一条纵向阅读轨道中依次呈现解答卡片、完成操作和同题追问记录的内容序列；其中任何部分都不拥有独立的纵向滚动区域。
_Avoid_: 回答框、嵌套滚动区

**思考过程**:
模型在形成主解答前产生的补充推理信息；默认以单行状态呈现，只有用户明确展开时才显示正文。
_Avoid_: 主解答、实时展开区
